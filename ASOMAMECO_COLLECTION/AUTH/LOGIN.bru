meta {
  name: LOGIN
  type: http
  seq: 1
}

post {
  url: {{URL}}auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "asoadmin.meco@example.com",
    "password": "123"
  }
}

script:post-response {
  bru.setEnvVar("TOKEN", res.body.token);
}
